{
  "name": "@services/event-examples",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "dependencies": {
    "aws-sdk": "^2.892.0"
  },
  "scripts": {
    "wipe-temp": "sudo rm -rf .temp/ .serverless/",
    "wipe": "npm run wipe-temp && sudo rm -rf node_modules/",
    "update-dependencies": "npx -y npm-check-updates -u",
    "check-lint": "npx -y eslint --max-warnings 0 --fix-dry-run .",
    "sls": "serverless",
    "serverless": "serverless",
    "offline:debug:global": "node --inspect=0.0.0.0:9229 $(npm config get prefix)/bin/sls offline start",
    "offline": "node --inspect=0.0.0.0:9229 $INIT_CWD/../../../node_modules/.bin/sls offline start",
    "test": "jest",
    "aws:sqs": "aws --endpoint-url http://queue-container:9324 --region us-east-1 sqs"
  },
  "scriptsDescriptions": {
    "EXPLANATION": "dummy object only used to comment built scripts(will be ignored by npm)",
    "wipe-temp": "remove all temporary files in service folder",
    "wipe": "remove all temporary files and possible installed modules in service folder(dependencies should be hoisted using workspaces. TODO: check in the future if it is a npm bug))",
    "update-dependencies": "[BEWARE OF BREAKING CHANGES]script for bumping all dependencies to the latest versions",
    "check-lint": "run eslint script to check for linting errors",
    "sls": "alias for serverless command(further command will be applied. E.g: npm run sls deploy -- --stage local ...)",
    "serverless": "alias for serverless command(further command will be applied. E.g: npm run serverless deploy -- --stage local ...)",
    "offline:debug:global": "[DEPRECATED:USE LOCAL NPM BIN] script used to start serverless offline with docker-compatible vscode debugger(Referencing global node_modules bin)",
    "offline": "alias for sls offline command with debugger(script used to start serverless offline with docker-compatible vscode debugger, referencing local node_modules bin)",
    "test": "test command",
    "aws:sqs": "alias for local container elasticmq aws-cli command(requires a double dash '--' to isolate npm script options from command(aws-cli) options. E.g: npm run aws:sqs create-queue -- --queue-name ...)"
  },
  "author": "",
  "license": "ISC"
}
